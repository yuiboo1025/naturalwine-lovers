<div class='container my-4'>
  <div class='row'>
    <div class='col text-center mx-auto', style="color:#3d83bf;">
      <h4>みんなのナチュール記録🍷<span class="h5"> (<%= @wines.count %>件)</span></h4>
    </div>
  </div>
  
      <%= render 'admin/wines/index', wines: @wines, genres: @genres %>
      
      <div class="d-flex justify-content-center mt-5">
        <%#= paginate @wines %>
      </div>

</div>
