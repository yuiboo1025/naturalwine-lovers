<div class="container my-4">
  <div class="card rounded col-6 mx-auto" style="background-color:#fcfced;" >
    <div class="row">
      <div class="col-12 p-0">
        <div class="card-header">
          <h4 class="text-center">ジャンル一覧・追加</h4>
        </div>
        <div class="text-center p-3">
          <%=form_with model: @genre,url: admin_genres_path,method: :post, local: true do |f| %>
            <%= f.text_field :genre_name, style:"background-color:#fcfced" %>
            <%= f.submit"新規登録",class: "btn pr-2 pl-2", style:"color:#ffffff; background-color:#20b2aa;" %>
          <% end %>
        </div>
        <div class="text-center text_red pt-4">
          <h6>※ジャンルを削除すると、紐づけられていた投稿がすべて削除されます</h6>
        </div>
        <table class="table mt-2 mb-0" style="background-color:#fcfced">
          <thead class="text-center table-active">
            <tr>
              <th style="width:60%">ジャンル</th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody class="text-center">
            <%@genres.each do |genre|%>
              <tr>
                <td><%=genre.genre_name %></td>
                <td><%= link_to "Edit",edit_admin_genre_path(genre), class: "btn pr-3 pl-3", style:"color:#ffffff; background-color:#20b2aa;" %></td>
                <td><%= link_to "Delete",admin_genre_path(genre), method: :delete, class: "btn btn-danger pr-3 pl-3", "data-confirm" => "本当に削除しますか？"%></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
