<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>NaturNote</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

  </head>

  <body style='background-color:#f7f6ed'>
    
    <div class="naturnote_container">
      <div class = "header" style="background-color:#f7f6ed">
        <%= render 'layouts/header' %>
      </div>

      <main>
        <div class="naturnote_main">
          <div class="flash-message">
            <%#　緑　%>
            <%= flash_message(flash[:success], :success) if flash[:success] %>
            <%#　赤　%>
            <%= flash_message(flash[:error], :danger) if flash[:error] %>
            <%#　黄　%>
            <%= flash_message(flash[:alert], :danger) if flash[:alert] %>
            <%#　青　%>
            <%= flash_message(flash[:notice], :info) if flash[:notice] %>
          </div>
          <%= yield %>
        </div>
      </main>
      
      <div class="footer-space"></div>
    
    <%#
    <footer>
      <div class="d-flex justify-content-center my-3">
      2023 NatureNote
      </div>
    </footer>
    %>
    
    </div>
  </body>
</html>

