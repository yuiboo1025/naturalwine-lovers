        <%= form_with model: [wine, comment], local: false do |f| %>
          <%= f.text_area :comment, rows: '3', cols: '50', placeholder: "コメントをここに" %>
          <%= f.submit "送信する" %>
        <% end %>