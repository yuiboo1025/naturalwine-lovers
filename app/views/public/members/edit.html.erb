<div class="container my-4">

  <div class="row justify-content-center">
    <div class="col-sm-md-7">
      <h3><%= @member.name %>のユーザー登録情報編集</h3>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-sm-md-7">
      <h6 class="text_red">※の項目は入力が必須です</h6>
    </div>
  </div>

  
  <div class="lg_size">
    <%= form_with model: @member do |f| %>
      <div class="row justify-content-center">
  			 <div class ="col-sm-md-7">
  			   <table class="table table-bordered">
  			     <tr>
                <th class="table-active">プロフィール画像</th>
                <td><%= f.file_field :profile_image, accept: "image/*", :placeholder =>"#{@member.profile_image}" %></td>
              </tr>
              <tr>
                <th class="table-active"><span class="text_red">※</span>ユーザー名(50字以内)</th>
                <td><%= f.text_field :name, :placeholder =>"#{@member.name}" %></td>
              </tr>
              <tr>
                <th class="table-active"><span class="text_red">※</span>メールアドレス</th>
                <td><%= f.email_field :email, autofocus: true, autocomplete: "email", :placeholder =>"#{@member.email}" %></td>
              </tr>
              <tr>
                <th class="table-active">好きなワインのジャンル</th>
                <td>
                  <%= f.select :favorite_genre, Member.favorite_genres.keys, {}, :placeholder =>"#{@member.favorite_genre}" %>
                </td>
              </tr>
              <tr>
                <th class="table-active">在住県</th>
                <td>
                  <%= f.select :prefecture, Member.prefectures.keys, {}, :placeholder =>"#{@member.prefecture}" %>
                </td>
              </tr>
              <tr>
                <th class="table-active">紹介文</th>
                <td><%= f.text_area :introduction, :placeholder =>"#{@member.introduction}", size: "30×50" %></td>
              </tr>
           </table>
         </div>
      </div>
  
      <div class="row justify-content-center">
        <div class ="col-sm-md-7 text-center">
          <%= f.submit "→ Update", class:"btn btn-info px-5 py-1 member_edit__update_btn" %>
        </div>
      </div>
      <div class="row justify-content-center mt-5">
        <div class="col-sm-md-7 text-left">
          <h6>※退会を希望する方はこちらからこちらから手続きを行なってください</h6>
        </div>
      </div>
      <div class="row justify-content-center">
        <div class="col-sm-md-6 text-right">
          <%= link_to "→退会する", confirm_unsubscribe_path, class: "btn px-3 py-1 member_edit__withdraw_btn" %>
          <%= link_to "→Back", :back, class:"btn px-3 py-1 ml-3 member_edit__back_btn" %>
        </div>
      </div>
    <% end %>
  </div>
  
  
  <div class="md_size">
    <%= form_with model: @member do |f| %>
      <div class="row justify-content-center">
  			 <div class ="col-sm-md-7">
  			   <div class="field my-3">
             プロフィール画像<br />
             <%= f.file_field :profile_image, accept: "image/*", :placeholder =>"#{@member.profile_image}" %>
          </div>
          <div class="field my-3">
            <span class="text_red">※</span>ユーザー名(50字以内)<br />
            <%= f.text_field :name, :placeholder =>"#{@member.name}" %>
          </div>
          <div class="field my-3">
            <span class="text_red">※</span>メールアドレス<br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", :placeholder =>"#{@member.email}" %>
          </div>
          <div class="field my-3">
             好きなワインのジャンル<br />
             <%= f.select :favorite_genre, Member.favorite_genres.keys, {}, :placeholder =>"#{@member.favorite_genre}" %>
          </div>
          <div class="field my-3">
             在住県<br />
             <%= f.select :prefecture, Member.prefectures.keys, {}, :placeholder =>"#{@member.prefecture}" %>
          </div>
          <div class="field my-3">
             紹介文<br />
             <%= f.text_area :introduction, :placeholder =>"#{@member.introduction}", size: "30×50" %>
          </div>
  			 </div>
  		</div>
  		
  		<div class="row justify-content-center">
        <div class ="col-sm-md-7 text-center">
          <%= f.submit "→ Update", class:"btn btn-info px-5 py-1 member_edit__update_btn" %>
        </div>
      </div>
      <div class="row justify-content-center mt-5">
        <div class="col-sm-md-7 text-left">
          <h6>※退会を希望する方はこちらからこちらから手続きを行なってください</h6>
        </div>
      </div>
      <div class="row justify-content-center">
        <div class="col-sm-md-6 text-right">
          <%= link_to "→退会する", confirm_unsubscribe_path, class: "btn px-3 py-1 member_edit__withdraw_btn" %>
          <%= link_to "→Back", :back, class:"btn px-3 py-1 ml-3 member_edit__back_btn" %>
        </div>
      </div>
    <% end %>
  </div>
  
  
</div>
