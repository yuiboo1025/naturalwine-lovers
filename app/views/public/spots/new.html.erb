<!-- ナチュール記録ボタン -->
<%= render 'public/wines/post_wine-btn' %>

<div class="container my-4">
  <div class="row mb-3">
    <h4>①投稿する店を選択</h4></br>
    <h5>(選択しない場合、そのままcreateボタンを押してください)</h5>
  </div>

<%= render 'spot_form', spot: @spot, method:spots_path %>

</div>