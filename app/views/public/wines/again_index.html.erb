<div class='container my-4'>
  
  <%= render 'member_info', member: @member %>
  
  <div class="row">
    <h5 class="col-sm-md text-center mx-auto text_blue">
      <%= @member.name %>のまた飲みたいナチュール🍷<span> (<%= @again_wines.count %>件)</span>
    </h5>
  </div>

  <%= render 'index', wines: @again_wines, member: @member, genres: @genres %>

</div>